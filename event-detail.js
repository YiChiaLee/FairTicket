const params=new URLSearchParams(location.search);const id=params.get("id")||"aespa";const event=FT_EVENTS.find(e=>e.id===id)||FT_EVENTS[3];document.querySelector("#detailTitle").textContent=event.title;document.querySelector("#detailType").textContent=event.type.toUpperCase();document.querySelector("#detailMeta").textContent=`${event.date} · ${event.place} · ${event.price||"Price varies"}`;const poster=document.querySelector("#detailPoster");if(event.image){poster.style.backgroundImage=`url('${event.image}')`;}else{poster.innerHTML="<div class='placeholder' style='height:100%'>◇</div>";}