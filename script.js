const grid=document.querySelector("#featuredGrid");FT_EVENTS.forEach(e=>{const card=document.createElement("article");card.className="event-card";card.innerHTML=eventCardHTML(e);card.addEventListener("click",()=>location.href=`event-detail.html?id=${e.id}`);grid.appendChild(card);});